[workspace]
members = ["bog_types"]

[package]
name = "bog"
version = "0.1.0"
authors = ["Matthew Norman"]
edition = "2024"
description = "A standard library alternative"
readme = "README.md"
homepage = "https://github.com/rtthw/bog"
repository = "https://github.com/rtthw/bog"
license-file = "LICENSE"

[[bin]]
name = "test"

[features]
default = ["all"]
all = []

[dependencies]
bog-types = { path = "bog_types" }
ration = "*"
